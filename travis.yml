dist: trusty
cache: bundler
language: ruby
os: osx

rvm:
  - 2.3.3
  - 2.4.2
  - 2.5.0

services:
  - mysql

before_install:
  - mysql -e 'CREATE DATABASE IF NOT EXISTS template_test;'

script:
  - bundle exec rails db:migrate RAILS_ENV=test
  - bundle exec rspec